import { Typography } from '../Typography';

export function BreadcrumbsSeparator() {
    return (
        <Typography className="uitw-text-muted-foreground/30 dark:uitw-text-muted-foreground/60">{'/'}</Typography>
    );
}
