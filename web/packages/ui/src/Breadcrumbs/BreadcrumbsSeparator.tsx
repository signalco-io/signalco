import { Typography } from '@signalco/ui-primitives/Typography';

export function BreadcrumbsSeparator() {
    return (
        <Typography className="text-muted-foreground/30 dark:text-muted-foreground/60">{'/'}</Typography>
    );
}
