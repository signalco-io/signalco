{
    "$schema": "https://turbo.build/schema.json",
    "extends": [
        "//"
    ],
    "pipeline": {
        "build": {
            "dependsOn": [
                "^build"
            ],
            "outputs": [
                ".next/**",
                "public/*.js"
            ],
            "env": [
                "COSMOSDB_CONNECTION_STRING",
                "OPENAI_API_KEY",
                "ACS_CONNECTION_STRING"
            ]
        }
    }
}
