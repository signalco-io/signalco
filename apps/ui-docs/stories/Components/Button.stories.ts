import { Button } from "@signalco/ui";
import { StoryObj } from "@storybook/react";

export default { component: Button, tags: ['docsPage'] };
type Story = StoryObj<typeof Button>;

export const Default: Story = {};